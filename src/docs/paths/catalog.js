const error400 = { description: 'Validation error', content: { 'application/json': { schema: { $ref: '#/components/schemas/ErrorResponse' } } } };
const error401 = { description: 'Unauthorized', content: { 'application/json': { schema: { $ref: '#/components/schemas/ErrorResponse' } } } };
const error403 = { description: 'Forbidden', content: { 'application/json': { schema: { $ref: '#/components/schemas/ErrorResponse' } } } };
const error404 = { description: 'Not found', content: { 'application/json': { schema: { $ref: '#/components/schemas/ErrorResponse' } } } };
const error409 = { description: 'Conflict', content: { 'application/json': { schema: { $ref: '#/components/schemas/ErrorResponse' } } } };
const error500 = { description: 'Internal server error', content: { 'application/json': { schema: { $ref: '#/components/schemas/ErrorResponse' } } } };

export const catalogPaths = {
  '/api/v1/categories': {
    post: {
      tags: ['Categories'],
      summary: 'Create category',
      description: 'Requires `category:create`.',
      operationId: 'createCategory',
      security: [{ bearerAuth: [] }],
      requestBody: {
        required: true,
        content: {
          'application/json': {
            schema: {
              type: 'object',
              required: ['name', 'slug'],
              properties: {
                name: { type: 'string', minLength: 1, maxLength: 255, example: 'Electronics' },
                slug: { type: 'string', pattern: '^[a-z0-9-]+$', minLength: 1, maxLength: 100, example: 'electronics' },
                description: { type: 'string', maxLength: 1000 },
                parentId: { type: 'string', format: 'uuid', nullable: true },
                sortOrder: { type: 'integer', default: 0 },
                isActive: { type: 'boolean', default: true },
              },
            },
          },
        },
      },
      responses: {
        201: { description: 'Category created', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { $ref: '#/components/schemas/Category' }, message: { type: 'string' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        409: error409,
        500: error500,
      },
    },
    get: {
      tags: ['Categories'],
      summary: 'List categories',
      description: 'Requires `category:read`.',
      operationId: 'listCategories',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'page', in: 'query', schema: { type: 'integer', minimum: 1, default: 1 } },
        { name: 'limit', in: 'query', schema: { type: 'integer', minimum: 1, maximum: 100, default: 20 } },
        { name: 'search', in: 'query', schema: { type: 'string', maxLength: 255 } },
        { name: 'isActive', in: 'query', schema: { type: 'boolean' } },
        { name: 'parentId', in: 'query', schema: { type: 'string', format: 'uuid' } },
        { name: 'sortBy', in: 'query', schema: { type: 'string', enum: ['name', 'slug', 'sortOrder', 'createdAt', 'updatedAt', 'isActive'], default: 'sortOrder' } },
        { name: 'sortOrder', in: 'query', schema: { type: 'string', enum: ['asc', 'desc'], default: 'asc' } },
      ],
      responses: {
        200: { description: 'Categories retrieved', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { type: 'array', items: { $ref: '#/components/schemas/Category' } }, meta: { $ref: '#/components/schemas/PaginationMeta' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        500: error500,
      },
    },
  },
  '/api/v1/categories/tree': {
    get: {
      tags: ['Categories'],
      summary: 'Get category tree',
      description: 'Requires `category:read`. Returns hierarchical tree. Must be called before /categories/{id} to avoid param collision.',
      operationId: 'getCategoryTree',
      security: [{ bearerAuth: [] }],
      responses: {
        200: { description: 'Category tree', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { type: 'array', items: { $ref: '#/components/schemas/Category' } }, message: { type: 'string' } } } } } },
        401: error401,
        403: error403,
        500: error500,
      },
    },
  },
  '/api/v1/categories/{id}': {
    get: {
      tags: ['Categories'],
      summary: 'Get category by ID',
      description: 'Requires `category:read`.',
      operationId: 'getCategory',
      security: [{ bearerAuth: [] }],
      parameters: [{ name: 'id', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } }],
      responses: {
        200: { description: 'Category retrieved', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { $ref: '#/components/schemas/Category' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
    patch: {
      tags: ['Categories'],
      summary: 'Update category',
      description: 'Requires `category:update`. At least one field.',
      operationId: 'updateCategory',
      security: [{ bearerAuth: [] }],
      parameters: [{ name: 'id', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } }],
      requestBody: {
        required: true,
        content: { 'application/json': { schema: { type: 'object', minProperties: 1, properties: { name: { type: 'string', minLength: 1, maxLength: 255 }, slug: { type: 'string', pattern: '^[a-z0-9-]+$' }, description: { type: 'string', maxLength: 1000 }, parentId: { type: 'string', format: 'uuid', nullable: true }, sortOrder: { type: 'integer' }, isActive: { type: 'boolean' } } } } },
      },
      responses: {
        200: { description: 'Category updated', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { $ref: '#/components/schemas/Category' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
    delete: {
      tags: ['Categories'],
      summary: 'Delete category',
      description: 'Requires `category:delete`.',
      operationId: 'deleteCategory',
      security: [{ bearerAuth: [] }],
      parameters: [{ name: 'id', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } }],
      responses: {
        200: { description: 'Category deleted', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { type: 'object', nullable: true } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
  },
  '/api/v1/products': {
    post: {
      tags: ['Products'],
      summary: 'Create product',
      description: 'Requires `product:create`.',
      operationId: 'createProduct',
      security: [{ bearerAuth: [] }],
      requestBody: {
        required: true,
        content: {
          'application/json': {
            schema: {
              type: 'object',
              required: ['name'],
              properties: {
                name: { type: 'string', minLength: 1, maxLength: 255, example: 'Wireless Headphones' },
                description: { type: 'string', maxLength: 5000 },
                brand: { type: 'string', maxLength: 100 },
                status: { type: 'string', enum: ['ACTIVE', 'INACTIVE', 'DRAFT', 'ARCHIVED'], default: 'DRAFT' },
                basePrice: { type: 'string', pattern: '^\\d+(\\.\\d{1,2})?$', example: '99.99' },
                categories: { type: 'array', items: { type: 'string', format: 'uuid' } },
                primaryCategoryId: { type: 'string', format: 'uuid' },
              },
            },
          },
        },
      },
      responses: {
        201: { description: 'Product created', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { $ref: '#/components/schemas/Product' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        500: error500,
      },
    },
    get: {
      tags: ['Products'],
      summary: 'List products',
      description: 'Requires `product:read`. Supports filtering, search, pagination.',
      operationId: 'listProducts',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'page', in: 'query', schema: { type: 'integer', minimum: 1, default: 1 } },
        { name: 'limit', in: 'query', schema: { type: 'integer', minimum: 1, maximum: 100, default: 20 } },
        { name: 'search', in: 'query', schema: { type: 'string', maxLength: 255 } },
        { name: 'status', in: 'query', schema: { type: 'string', enum: ['ACTIVE', 'INACTIVE', 'DRAFT', 'ARCHIVED'] } },
        { name: 'categoryId', in: 'query', schema: { type: 'string', format: 'uuid' } },
        { name: 'minPrice', in: 'query', schema: { type: 'number' } },
        { name: 'maxPrice', in: 'query', schema: { type: 'number' } },
        { name: 'sku', in: 'query', schema: { type: 'string', maxLength: 100 } },
        { name: 'barcode', in: 'query', schema: { type: 'string', maxLength: 100 } },
        { name: 'sortBy', in: 'query', schema: { type: 'string', enum: ['name', 'brand', 'status', 'basePrice', 'createdAt', 'updatedAt'], default: 'createdAt' } },
        { name: 'sortOrder', in: 'query', schema: { type: 'string', enum: ['asc', 'desc'], default: 'desc' } },
      ],
      responses: {
        200: { description: 'Products retrieved', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { type: 'array', items: { $ref: '#/components/schemas/Product' } }, meta: { $ref: '#/components/schemas/PaginationMeta' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        500: error500,
      },
    },
  },
  '/api/v1/products/{id}': {
    get: {
      tags: ['Products'],
      summary: 'Get product by ID',
      description: 'Requires `product:read`.',
      operationId: 'getProduct',
      security: [{ bearerAuth: [] }],
      parameters: [{ name: 'id', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } }],
      responses: {
        200: { description: 'Product retrieved', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { $ref: '#/components/schemas/Product' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
    patch: {
      tags: ['Products'],
      summary: 'Update product',
      description: 'Requires `product:update`.',
      operationId: 'updateProduct',
      security: [{ bearerAuth: [] }],
      parameters: [{ name: 'id', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } }],
      requestBody: {
        required: true,
        content: { 'application/json': { schema: { type: 'object', minProperties: 1, properties: { name: { type: 'string', minLength: 1, maxLength: 255 }, description: { type: 'string', maxLength: 5000 }, brand: { type: 'string', maxLength: 100 }, status: { type: 'string', enum: ['ACTIVE', 'INACTIVE', 'DRAFT', 'ARCHIVED'] }, basePrice: { type: 'string', pattern: '^\\d+(\\.\\d{1,2})?$' }, categories: { type: 'array', items: { type: 'string', format: 'uuid' } }, primaryCategoryId: { type: 'string', format: 'uuid' } } } } },
      },
      responses: {
        200: { description: 'Product updated', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { $ref: '#/components/schemas/Product' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
    delete: {
      tags: ['Products'],
      summary: 'Delete product',
      description: 'Requires `product:delete`.',
      operationId: 'deleteProduct',
      security: [{ bearerAuth: [] }],
      parameters: [{ name: 'id', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } }],
      responses: {
        200: { description: 'Product deleted', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { type: 'object', nullable: true } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
  },
  '/api/v1/products/{productId}/categories': {
    post: {
      tags: ['Products'],
      summary: 'Set product categories',
      description: 'Requires `product:update`. Replaces category associations.',
      operationId: 'setProductCategories',
      security: [{ bearerAuth: [] }],
      parameters: [{ name: 'productId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } }],
      requestBody: {
        required: true,
        content: { 'application/json': { schema: { type: 'object', required: ['categories'], properties: { categories: { type: 'array', minItems: 1, items: { type: 'string', format: 'uuid' } }, primaryCategoryId: { type: 'string', format: 'uuid' } } } } },
      },
      responses: {
        200: { description: 'Categories set', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { type: 'array', items: { type: 'object' } } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
    get: {
      tags: ['Products'],
      summary: 'Get product categories',
      description: 'Requires `product:read`.',
      operationId: 'getProductCategories',
      security: [{ bearerAuth: [] }],
      parameters: [{ name: 'productId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } }],
      responses: {
        200: { description: 'Product categories', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { type: 'array', items: { $ref: '#/components/schemas/Category' } } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
  },
  '/api/v1/products/{productId}/variants': {
    post: {
      tags: ['Variants'],
      summary: 'Create variant',
      description: 'Requires `product:create`. Mounted at /api/v1/products/:productId/variants.',
      operationId: 'createVariant',
      security: [{ bearerAuth: [] }],
      parameters: [{ name: 'productId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } }],
      requestBody: {
        required: true,
        content: {
          'application/json': {
            schema: {
              type: 'object',
              required: ['sku', 'price'],
              properties: {
                sku: { type: 'string', minLength: 1, maxLength: 100, example: 'WH-001-BLACK' },
                barcode: { type: 'string', maxLength: 100 },
                price: { type: 'string', pattern: '^\\d+(\\.\\d{1,2})?$', example: '99.99' },
                costPrice: { type: 'string', pattern: '^\\d+(\\.\\d{1,2})?$' },
                status: { type: 'string', enum: ['ACTIVE', 'INACTIVE', 'DRAFT'], default: 'DRAFT' },
                attributes: { type: 'array', items: { type: 'object', required: ['attributeDefinitionId', 'value'], properties: { attributeDefinitionId: { type: 'string', format: 'uuid' }, value: { type: 'string' } } } },
              },
            },
          },
        },
      },
      responses: {
        201: { description: 'Variant created', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { $ref: '#/components/schemas/ProductVariant' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        409: error409,
        500: error500,
      },
    },
    get: {
      tags: ['Variants'],
      summary: 'List variants',
      description: 'Requires `product:read`.',
      operationId: 'listVariants',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'productId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
        { name: 'page', in: 'query', schema: { type: 'integer', minimum: 1, default: 1 } },
        { name: 'limit', in: 'query', schema: { type: 'integer', minimum: 1, maximum: 100, default: 20 } },
        { name: 'status', in: 'query', schema: { type: 'string', enum: ['ACTIVE', 'INACTIVE', 'DRAFT'] } },
        { name: 'sortBy', in: 'query', schema: { type: 'string', enum: ['sku', 'price', 'status', 'createdAt', 'updatedAt'], default: 'createdAt' } },
        { name: 'sortOrder', in: 'query', schema: { type: 'string', enum: ['asc', 'desc'], default: 'asc' } },
      ],
      responses: {
        200: { description: 'Variants retrieved', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { type: 'array', items: { $ref: '#/components/schemas/ProductVariant' } }, meta: { $ref: '#/components/schemas/PaginationMeta' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
  },
  '/api/v1/products/{productId}/variants/{variantId}': {
    get: {
      tags: ['Variants'],
      summary: 'Get variant by ID',
      description: 'Requires `product:read`.',
      operationId: 'getVariant',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'productId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
        { name: 'variantId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
      ],
      responses: {
        200: { description: 'Variant retrieved', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { $ref: '#/components/schemas/ProductVariant' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
    patch: {
      tags: ['Variants'],
      summary: 'Update variant',
      description: 'Requires `product:update`. At least one field.',
      operationId: 'updateVariant',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'productId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
        { name: 'variantId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
      ],
      requestBody: {
        required: true,
        content: { 'application/json': { schema: { type: 'object', minProperties: 1, properties: { sku: { type: 'string', minLength: 1, maxLength: 100 }, barcode: { type: 'string', maxLength: 100 }, price: { type: 'string', pattern: '^\\d+(\\.\\d{1,2})?$' }, costPrice: { type: 'string', pattern: '^\\d+(\\.\\d{1,2})?$' }, status: { type: 'string', enum: ['ACTIVE', 'INACTIVE', 'DRAFT'] }, attributes: { type: 'array', items: { type: 'object' } } } } } },
      },
      responses: {
        200: { description: 'Variant updated', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { $ref: '#/components/schemas/ProductVariant' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
    delete: {
      tags: ['Variants'],
      summary: 'Delete variant',
      description: 'Requires `product:delete`.',
      operationId: 'deleteVariant',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'productId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
        { name: 'variantId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
      ],
      responses: {
        200: { description: 'Variant deleted', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { type: 'object', nullable: true } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
  },
  '/api/v1/products/{productId}/variants/{variantId}/attributes': {
    put: {
      tags: ['Variants'],
      summary: 'Set variant attributes',
      description: 'Requires `product:update`. Replaces attributes (PUT).',
      operationId: 'setVariantAttributes',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'productId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
        { name: 'variantId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
      ],
      requestBody: {
        required: true,
        content: { 'application/json': { schema: { type: 'object', required: ['attributes'], properties: { attributes: { type: 'array', items: { type: 'object', required: ['attributeDefinitionId', 'value'], properties: { attributeDefinitionId: { type: 'string', format: 'uuid' }, value: { type: 'string' } } } } } } } },
      },
      responses: {
        200: { description: 'Attributes set', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { type: 'array', items: { type: 'object' } } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
    get: {
      tags: ['Variants'],
      summary: 'Get variant attributes',
      description: 'Requires `product:read`.',
      operationId: 'getVariantAttributes',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'productId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
        { name: 'variantId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
      ],
      responses: {
        200: { description: 'Variant attributes', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { type: 'array', items: { type: 'object' } } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
  },
  '/api/v1/attributes': {
    post: {
      tags: ['Attributes'],
      summary: 'Create attribute definition',
      description: 'Requires `attribute:create`.',
      operationId: 'createAttribute',
      security: [{ bearerAuth: [] }],
      requestBody: {
        required: true,
        content: {
          'application/json': {
            schema: {
              type: 'object',
              required: ['name', 'code', 'dataType'],
              properties: {
                name: { type: 'string', minLength: 1, maxLength: 255, example: 'Color' },
                code: { type: 'string', pattern: '^[a-z0-9_-]+$', minLength: 1, maxLength: 50, example: 'color' },
                dataType: { type: 'string', enum: ['TEXT', 'NUMBER', 'BOOLEAN', 'OPTION'] },
                isRequired: { type: 'boolean', default: false },
                description: { type: 'string', maxLength: 1000 },
              },
            },
          },
        },
      },
      responses: {
        201: { description: 'Attribute created', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { $ref: '#/components/schemas/AttributeDefinition' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        409: error409,
        500: error500,
      },
    },
    get: {
      tags: ['Attributes'],
      summary: 'List attributes',
      description: 'Requires `attribute:read`.',
      operationId: 'listAttributes',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'page', in: 'query', schema: { type: 'integer', minimum: 1, default: 1 } },
        { name: 'limit', in: 'query', schema: { type: 'integer', minimum: 1, maximum: 100, default: 20 } },
        { name: 'search', in: 'query', schema: { type: 'string', maxLength: 255 } },
        { name: 'dataType', in: 'query', schema: { type: 'string', enum: ['TEXT', 'NUMBER', 'BOOLEAN', 'OPTION'] } },
        { name: 'sortBy', in: 'query', schema: { type: 'string', enum: ['name', 'code', 'dataType', 'isRequired', 'createdAt', 'updatedAt'], default: 'name' } },
        { name: 'sortOrder', in: 'query', schema: { type: 'string', enum: ['asc', 'desc'], default: 'asc' } },
      ],
      responses: {
        200: { description: 'Attributes retrieved', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { type: 'array', items: { $ref: '#/components/schemas/AttributeDefinition' } }, meta: { $ref: '#/components/schemas/PaginationMeta' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        500: error500,
      },
    },
  },
  '/api/v1/attributes/{id}': {
    get: {
      tags: ['Attributes'],
      summary: 'Get attribute by ID',
      description: 'Requires `attribute:read`.',
      operationId: 'getAttribute',
      security: [{ bearerAuth: [] }],
      parameters: [{ name: 'id', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } }],
      responses: {
        200: { description: 'Attribute retrieved', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { $ref: '#/components/schemas/AttributeDefinition' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
    patch: {
      tags: ['Attributes'],
      summary: 'Update attribute',
      description: 'Requires `attribute:update`. At least one field.',
      operationId: 'updateAttribute',
      security: [{ bearerAuth: [] }],
      parameters: [{ name: 'id', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } }],
      requestBody: {
        required: true,
        content: { 'application/json': { schema: { type: 'object', minProperties: 1, properties: { name: { type: 'string', minLength: 1, maxLength: 255 }, code: { type: 'string', pattern: '^[a-z0-9_-]+$' }, dataType: { type: 'string', enum: ['TEXT', 'NUMBER', 'BOOLEAN', 'OPTION'] }, isRequired: { type: 'boolean' }, description: { type: 'string', maxLength: 1000 } } } } },
      },
      responses: {
        200: { description: 'Attribute updated', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { $ref: '#/components/schemas/AttributeDefinition' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
    delete: {
      tags: ['Attributes'],
      summary: 'Delete attribute',
      description: 'Requires `attribute:delete`.',
      operationId: 'deleteAttribute',
      security: [{ bearerAuth: [] }],
      parameters: [{ name: 'id', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } }],
      responses: {
        200: { description: 'Attribute deleted', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { type: 'object', nullable: true } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
  },
  '/api/v1/attributes/{attributeId}/values': {
    post: {
      tags: ['Attributes'],
      summary: 'Create attribute value',
      description: 'Requires `attribute:update`.',
      operationId: 'createAttributeValue',
      security: [{ bearerAuth: [] }],
      parameters: [{ name: 'attributeId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } }],
      requestBody: {
        required: true,
        content: { 'application/json': { schema: { type: 'object', required: ['value', 'displayName'], properties: { value: { type: 'string', minLength: 1, maxLength: 255, example: 'red' }, displayName: { type: 'string', minLength: 1, maxLength: 255, example: 'Red' }, sortOrder: { type: 'integer', default: 0 }, isActive: { type: 'boolean', default: true } } } } },
      },
      responses: {
        201: { description: 'Attribute value created', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { $ref: '#/components/schemas/AttributeValue' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
    get: {
      tags: ['Attributes'],
      summary: 'List attribute values',
      description: 'Requires `attribute:read`.',
      operationId: 'listAttributeValues',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'attributeId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
        { name: 'page', in: 'query', schema: { type: 'integer', minimum: 1, default: 1 } },
        { name: 'limit', in: 'query', schema: { type: 'integer', minimum: 1, maximum: 100, default: 50 } },
      ],
      responses: {
        200: { description: 'Attribute values retrieved', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { type: 'array', items: { $ref: '#/components/schemas/AttributeValue' } }, meta: { $ref: '#/components/schemas/PaginationMeta' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
  },
  '/api/v1/attributes/{attributeId}/values/{valueId}': {
    get: {
      tags: ['Attributes'],
      summary: 'Get attribute value',
      description: 'Requires `attribute:read`.',
      operationId: 'getAttributeValue',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'attributeId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
        { name: 'valueId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
      ],
      responses: {
        200: { description: 'Attribute value retrieved', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { $ref: '#/components/schemas/AttributeValue' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
    patch: {
      tags: ['Attributes'],
      summary: 'Update attribute value',
      description: 'Requires `attribute:update`. At least one field.',
      operationId: 'updateAttributeValue',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'attributeId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
        { name: 'valueId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
      ],
      requestBody: {
        required: true,
        content: { 'application/json': { schema: { type: 'object', minProperties: 1, properties: { value: { type: 'string', minLength: 1, maxLength: 255 }, displayName: { type: 'string', minLength: 1, maxLength: 255 }, sortOrder: { type: 'integer' }, isActive: { type: 'boolean' } } } } },
      },
      responses: {
        200: { description: 'Attribute value updated', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { $ref: '#/components/schemas/AttributeValue' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
    delete: {
      tags: ['Attributes'],
      summary: 'Delete attribute value',
      description: 'Requires `attribute:delete`.',
      operationId: 'deleteAttributeValue',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'attributeId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
        { name: 'valueId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
      ],
      responses: {
        200: { description: 'Attribute value deleted', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { type: 'object', nullable: true } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
  },
  '/api/v1/products/{productId}/images': {
    post: {
      tags: ['Product Images'],
      summary: 'Upload product image',
      description: 'Requires `product:update`. Multipart/form-data with field `image` (jpeg/png/webp/gif, max 10MB). Optional altText, sortOrder, isPrimary.',
      operationId: 'createProductImage',
      security: [{ bearerAuth: [] }],
      parameters: [{ name: 'productId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } }],
      requestBody: {
        required: true,
        content: {
          'multipart/form-data': {
            schema: {
              type: 'object',
              properties: {
                image: { type: 'string', format: 'binary' },
                altText: { type: 'string', maxLength: 255 },
                sortOrder: { type: 'integer', default: 0 },
                isPrimary: { type: 'boolean', default: false },
              },
              required: ['image'],
            },
          },
        },
      },
      responses: {
        201: { description: 'Image created', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { $ref: '#/components/schemas/ProductImage' } } } } } },
        400: { description: 'Validation, file too large, or invalid file type', content: { 'application/json': { schema: { $ref: '#/components/schemas/ErrorResponse' } } } },
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
    get: {
      tags: ['Product Images'],
      summary: 'List product images',
      description: 'Requires `product:read`.',
      operationId: 'listProductImages',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'productId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
        { name: 'page', in: 'query', schema: { type: 'integer', minimum: 1, default: 1 } },
        { name: 'limit', in: 'query', schema: { type: 'integer', minimum: 1, maximum: 100, default: 20 } },
      ],
      responses: {
        200: { description: 'Product images', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { type: 'array', items: { $ref: '#/components/schemas/ProductImage' } }, meta: { $ref: '#/components/schemas/PaginationMeta' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
  },
  '/api/v1/products/{productId}/variants/{variantId}/images': {
    post: {
      tags: ['Product Images'],
      summary: 'Upload variant image',
      description: 'Requires `product:update`. Multipart with `image`.',
      operationId: 'createVariantImage',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'productId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
        { name: 'variantId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
      ],
      requestBody: {
        required: true,
        content: { 'multipart/form-data': { schema: { type: 'object', required: ['image'], properties: { image: { type: 'string', format: 'binary' }, altText: { type: 'string', maxLength: 255 }, sortOrder: { type: 'integer', default: 0 }, isPrimary: { type: 'boolean', default: false } } } } },
      },
      responses: {
        201: { description: 'Variant image created', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { $ref: '#/components/schemas/ProductImage' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
    get: {
      tags: ['Product Images'],
      summary: 'List variant images',
      description: 'Requires `product:read`.',
      operationId: 'listVariantImages',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'productId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
        { name: 'variantId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
        { name: 'page', in: 'query', schema: { type: 'integer', minimum: 1, default: 1 } },
        { name: 'limit', in: 'query', schema: { type: 'integer', minimum: 1, maximum: 100, default: 20 } },
      ],
      responses: {
        200: { description: 'Variant images', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { type: 'array', items: { $ref: '#/components/schemas/ProductImage' } } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
  },
  '/api/v1/products/{productId}/images/{imageId}': {
    get: {
      tags: ['Product Images'],
      summary: 'Get product image',
      description: 'Requires `product:read`.',
      operationId: 'getProductImage',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'productId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
        { name: 'imageId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
      ],
      responses: {
        200: { description: 'Image retrieved', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { $ref: '#/components/schemas/ProductImage' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
    patch: {
      tags: ['Product Images'],
      summary: 'Update product image',
      description: 'Requires `product:update`. At least one field.',
      operationId: 'updateProductImage',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'productId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
        { name: 'imageId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
      ],
      requestBody: {
        required: true,
        content: { 'application/json': { schema: { type: 'object', minProperties: 1, properties: { altText: { type: 'string', maxLength: 255 }, sortOrder: { type: 'integer' }, isPrimary: { type: 'boolean' } } } } },
      },
      responses: {
        200: { description: 'Image updated', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { $ref: '#/components/schemas/ProductImage' } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
    delete: {
      tags: ['Product Images'],
      summary: 'Delete product image',
      description: 'Requires `product:delete`.',
      operationId: 'deleteProductImage',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'productId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
        { name: 'imageId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
      ],
      responses: {
        200: { description: 'Image deleted', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { type: 'object', nullable: true } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
  },
  '/api/v1/products/{productId}/images/{imageId}/file': {
    get: {
      tags: ['Product Images'],
      summary: 'Get product image file (binary)',
      description: 'Requires `product:read`. Streams file, tenant-isolated. Returns binary image.',
      operationId: 'getProductImageFile',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'productId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
        { name: 'imageId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
      ],
      responses: {
        200: { description: 'Image file stream', content: { 'image/jpeg': { schema: { type: 'string', format: 'binary' } }, 'image/png': { schema: { type: 'string', format: 'binary' } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
  },
  '/api/v1/products/{productId}/images/{imageId}/signed-url': {
    get: {
      tags: ['Product Images'],
      summary: 'Get signed URL for product image',
      description: 'Requires `product:read`. Generates HMAC signed URL with expiry.',
      operationId: 'getProductImageSignedUrl',
      security: [{ bearerAuth: [] }],
      parameters: [
        { name: 'productId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
        { name: 'imageId', in: 'path', required: true, schema: { type: 'string', format: 'uuid' } },
      ],
      responses: {
        200: { description: 'Signed URL', content: { 'application/json': { schema: { type: 'object', properties: { success: { type: 'boolean', example: true }, data: { type: 'object', properties: { url: { type: 'string' }, expires: { type: 'string' }, signature: { type: 'string' } } } } } } } },
        400: error400,
        401: error401,
        403: error403,
        404: error404,
        500: error500,
      },
    },
  },
};
